<%= form_for(@wait_status) do |f| %>
  <% if @wait_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wait_status.errors.count, "error") %> prohibited this wait_status from being saved:</h2>

      <ul>
      <% @wait_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :table_type %><br>
    <%= f.select :table_type, collection: WaitStatus::TABLE_TYPES %>
  </div>
  <%= f.hidden_field :member_id, :value => @member.id %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
